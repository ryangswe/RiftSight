// Injected by build.mjs via esbuild's `define` option — a build-time
// literal, not a runtime global. Empty string when RIFTSIGHT_RELAY_URL
// wasn't set for that build.
declare const __RIFTSIGHT_RELAY_URL__: string;
