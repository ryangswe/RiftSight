{
  "name": "riftsight-card-inspector",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "bundle": "node build.mjs",
    "build": "npm run typecheck && npm run bundle",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@riftsight/protocol": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
